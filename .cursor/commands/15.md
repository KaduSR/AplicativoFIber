arefa 13: Corrigir Importação do useAuth
Objetivo: Corrigir o crash (0 , _AuthContext.useAuth) is not a function no app/_layout.tsx alterando a importação para usar o hook correto (hooks/useAuth.tsx).

Contexto: O layout raiz está a falhar ao tentar ler o estado de autenticação porque está a importar a função useAuth do ficheiro errado.

Modificações no seu app/_layout.tsx
Instruções:

Abra o ficheiro: AplicativoFIber/app/_layout.tsx

Encontre a Linha de Importação (Errada): Procure pela linha de import que está a importar useAuth do contexto:

TypeScript

// Em: app/_layout.tsx

// (A SUA LINHA ERRADA)
import { AuthProvider, useAuth } from '@/contexts/AuthContext';
// ...
Substitua pelas Importações Corretas: Precisamos de separar as importações. O AuthProvider vem do contexto, mas o useAuth vem do hook.

Substitua a linha errada por estas duas linhas:

TypeScript

// Em: app/_layout.tsx

// (O SEU CÓDIGO CORRIGIDO)
import { AuthProvider } from '@/contexts/AuthContext'; // <--- O Provider
import { useAuth } from '@/hooks/useAuth';           // <--- O Hook

import { Stack, useRouter } from 'expo-router';
import { useEffect } from 'react';
// ...