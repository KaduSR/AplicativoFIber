Tarefa 14: Corrigir Chamada de Login na app/login.tsx
Objetivo: Modificar a tela app/login.tsx para que ela chame a função signIn (em vez de login) do useAuth hook.

Contexto: O erro login is not a function está a acontecer no frontend porque o AuthContext (através do useAuth) fornece { signIn, signOut, user, isLoading }, mas a tela de login está a tentar aceder a login.

Modificações no seu app/login.tsx
Instruções:

Abra o ficheiro: AplicativoFIber/app/login.tsx

Encontre a chamada ao useAuth: No início do seu componente LoginScreen, encontre a linha onde você usa o useAuth.

Se estiver assim (ERRADO):

TypeScript

// Em: app/login.tsx

import { useAuth } from '@/hooks/useAuth';
// ...

export default function LoginScreen() {
  const { login, isLoading } = useAuth(); // <--- LINHA ERRADA
  // ...
Corrija para (CORRETO):

TypeScript

// Em: app/login.tsx

import { useAuth } from '@/hooks/useAuth';
// ...

export default function LoginScreen() {
  const { signIn, isLoading } = useAuth(); // <--- CORREÇÃO: Mudar 'login' para 'signIn'
  // ...
Encontre a Função que Chama o Login: Agora, encontre a função que é chamada quando o botão "Entrar" é pressionado (ex: handleLogin ou onSubmit).

Se estiver assim (ERRADO):

TypeScript

// Em: app/login.tsx

const handleLogin = async () => {
  // ...
  await login({ login: email, senha: password }); // <--- LINHA ERRADA
  // ...
};
Corrija para (CORRETO):

TypeScript

// Em: app/login.tsx

const handleLogin = async () => {
  // ...
  await signIn({ login: email, senha: password }); // <--- CORREÇÃO: Mudar 'login' para 'signIn'
  // ...
};