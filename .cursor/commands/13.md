Tarefa 13: Implementar o Layout Final do Perfil (Com Dados Reais)
Objetivo: Substituir o app/(tabs)/perfil.tsx temporário (Tarefa 11) pelo design final fornecido nas imagens, e conectá-lo aos dados reais do AuthContext.

Contexto: O backend (server.js) está a funcionar e a autenticar. O AuthContext (corrigido na Tarefa 12) está agora a fornecer o objeto user (com id_cliente, nome_cliente, etc.) para o aplicativo.

1. Ficheiro: app/(tabs)/perfil.tsx
Tarefa: Recriar a tela de Perfil para ser 100% fiel às imagens.

Requisitos Técnicos:

Obter Dados:

Importe e use o hook useAuth para obter const { user, signOut } = useAuth();.

O user agora contém os dados reais do cliente vindos do backend (Tarefa 05 e Tarefa 08).

Layout (Baseado na image_5a1c5f.png):

Renderize o cabeçalho "Meu Perfil".

Renderize o componente de avatar (use uma imagem placeholder se ainda não tivermos a foto do cliente).

Renderize o nome do cliente: user.nome_cliente.

Renderize o e-mail do cliente: user.email.

Crie a lista de botões de navegação (conforme a imagem): "Meus Dados", "Meus Contratos", "Minhas Faturas", "Notificações", "Meus Cartões".

(Nota: Estes botões ainda não precisam de navegar, apenas de existir no layout).

Layout (Baseado na image_5a1c7f.png):

No final da tela, adicione o botão vermelho "Sair".

Este botão deve chamar a função signOut quando pressionado, executando a lógica de logout que corrigimos na Tarefa 09 e Tarefa 10.

2. (Opcional) Ficheiro: contexts/AuthContext.tsx
Tarefa: Garantir que não estamos mais a usar os dados placeholder.

Instruções:

Verifique a sua função signIn no AuthContext.tsx.

Certifique-se de que removeu a chamada ao authService.mockLogin() de dentro do bloco catch (error), como fizemos na Tarefa 12. O catch deve agora apenas mostrar o Alert.alert("Erro no Login", error.message);.